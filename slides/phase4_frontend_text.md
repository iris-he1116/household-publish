# Phase 4 前半 勉強会スライド ― テキスト版

`phase4_frontend.pptx` の全文。**コピペ用**。
話す内容の調整や、Google スライド上での文字修正の元データとして使う。

各スライドの「話すこと」は発表者ノートにも入れてある（PowerPoint / Google スライドのノート欄に表示される）。

**最終更新: 2026-08-25（実装後の実測値を追記）**

---

## 実装から得た実測値（S6 / S8 / S9 に反映済み）

スライド作成後に3画面すべてを実装したため、以下の実測値が取れた。
**①②⑤は pptx に反映済み**。残りは口頭の補足材料として残してある。

> 数字は 2026-08-25 に `seed_dev_data.py` でリセットした後の状態。
> 発表時に別の数字が出ていたら、その場の値を読み上げれば足りる。

### 実測①：ブラウザは API を一度も叩いていない（→ S6 に反映したい）

ホーム画面を開いた状態で計測した結果。

```
ブラウザから叩いた API      : 0 件
画面に表示されているデータ   : 集計・カテゴリ別・支払い手段別・支出10件
```

`curl http://localhost:3000` でサーバーが返す HTML を見ると、既に数字が入っている。

```
「¥ 22,868」（合計）      → HTML に含まれる
「中華食材」（支出のメモ） → HTML に含まれる
```

つまり **ブラウザが JavaScript を動かす前から、データは画面に載っている**。
これが S4「サーバーコンポーネントはブラウザに JS を送らない」の実物。

### 実測②：refresh() で画面が更新される（→ S8 に反映したい）

入力フォームから支出を1件（外食 ¥2,580）登録したときの変化。**ページ遷移なし**。

| | 送信前 | 送信後 |
| :---- | :---- | :---- |
| 共有支出の合計 | ¥20,288 | **¥22,868** |
| 件数 | 9件 | **10件** |
| 外食カテゴリ | ¥2,800（1件） | **¥5,380（2件）** |
| 送金額 | ¥1,896 | **¥3,186** |
| 一覧に新規メモ | — | **あり** |
| ブラウザから叩いた API | 0 | **0** |

集計・グラフ・一覧が**同時に**更新される。個別に再取得するコードは1行も書いていない。
Server Action の中で `refresh()` を呼んだだけ。

### 実測③：422 が該当フィールドの下に出る（→ S8 に反映したい）

金額を空のまま送信したときの画面。

```
金額     [¥ 0]        ← 赤枠
         金額を入力してください

カテゴリ [選択 ▾]      ← 赤枠
         カテゴリを選択してください
```

画面全体のエラー表示にしていない。バックエンドが返す `detail[].loc` の2番目
（`["body", "amount"]` の `"amount"`）を使って、該当する入力欄に振り分けている。

Pydantic の英語メッセージ（`Input should be greater than 0`）はそのまま出さず、
`type`（`greater_than` など）から日本語を引いている。

### 実測④：React 19 はフォームを毎回自動リセットする（→ S9 に反映したい）

実装して初めて分かった挙動。

> **React 19 は Server Action の完了後、成功・失敗を問わずフォームを自動リセットする。**

放置すると、金額の入力ミス1つで**日付もカテゴリもメモも全部消える**。
対策として、失敗時だけ送信値を state に保持して `defaultValue` に戻している。

これも「ドキュメントを読んでも書いていない、動かして初めて分かること」の例。
S9（記事と公式が違った話）と同じ系統のネタとして使える。

### 実測⑤：Next.js 自身が「訓練データと違う」と警告していた（→ S9 の決定打）

`npm run dev` を実行すると、Next.js が `frontend/AGENTS.md` を**自動生成**する。中身：

```
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure
may all differ from your training data. Read the relevant guide in
`node_modules/next/dist/docs/` before writing any code.
Heed deprecation notices.
```

**「あなたの知っている Next.js ではない。書き始める前に同梱ドキュメントを読め」**と、
フレームワーク自身が言っている。しかも `node_modules` にドキュメント一式が同梱されている。

これは S9（記事と公式が違った話）の決定打になる。
「バージョン差が実際に問題になっている」ことを、フレームワーク側が認めている証拠。

実際に同梱ドキュメントを読んで確認したこと：

| 項目 | 分かったこと |
| :---- | :---- |
| 動的ルートの `params` | **Promise なので `await` が必要**（`const { ym } = await params`） |
| `searchParams` | 同様に Promise |

古い記事のままだと `params.ym` と書いてしまい動かない。

### 実測⑥：月の切り替えは URL に持たせればサーバーのままでいられる（→ S5 / S6 に反映したい）

月次清算画面の「◂ 前の月 / 次の月 ▸」の実装で判断したこと。

```
useState で月を持つ場合:
  → MonthNav がクライアントコンポーネントになる
  → 月が変わったら集計を取り直すため useEffect + fetch が必要
  → ローディング状態の管理も自前でやることになる

<Link href="/settlement/2026-06"> の場合:
  → URL が変わってページ全体がサーバーで再描画される
  → MonthNav はサーバーコンポーネントのまま
  → 集計はサーバーで取得済みの状態で届く
```

「月を選ぶ」は一見すると状態管理に見えるが、**URL に持たせればサーバーのままでいられる**。
S5「'use client' は葉に付ける」の応用例として使える。

実装した月次清算画面のクライアントコンポーネントは**ボタン1つだけ**（`SettlementActionButton`）。
集計表示・月ナビ・過去履歴はすべてサーバーコンポーネント。

### 実測⑦：フィルタを URL に持たせるとサーバーで絞り込める（→ S5 / S6 の決定打）

支出一覧の絞り込み（月 / カテゴリ / 支払い手段 / 支払者）を **URL のクエリパラメータ**で
表現した。`useState` は使っていない。

`curl` でサーバーが返す HTML を取ると、**件数と合計が既に絞り込まれている**。

| URL | サーバーが返した HTML の中身 |
| :---- | :---- |
| `/` | 11 件 / ¥25,468 |
| `/?category=1`（食費） | **5 件 / ¥13,134** |
| `/?method=cash`（現金） | **4 件 / ¥10,994** |
| `/?payer=2`（ひつじ） | **4 件 / ¥8,248** |
| `/?category=1&method=cash` | **3 件 / ¥8,414** |

ブラウザで JavaScript が動く前に、もう絞り込みが終わっている。

**もし `useState` で絞り込んでいたら**：
- そのコンポーネントがクライアント化する
- 条件が変わるたびに `useEffect` + `fetch` で取り直す必要がある
- ローディング状態も自前で管理する
- 絞り込んだ状態を共有・ブックマークできない

URL に持たせたことで、一覧テーブルは**サーバーコンポーネントのまま**でいられた。
クライアントなのは絞り込みの `<select>` と、行内編集・削除のボタンだけ。

### 実測⑧：ナビも「現在地の判定」だけがクライアント（→ S5 の補強）

3画面を行き来するナビを実装したときの判断。

「今どの画面にいるか」で見た目を変えるには `usePathname` が要る。
これはブラウザ側でしか分からないので `'use client'` が必要になる。

ただしクライアントにしたのは**ナビ本体だけ**：

```
app/layout.tsx              → サーバーのまま（ナビを置くだけ）
AppNavSection.tsx           → サーバー（未判定件数を API から取る）
  └─ AppNav.tsx             → ★クライアント★（usePathname で現在地判定）
```

未判定バッジの件数は `AppNavSection`（サーバー）が取って props で渡している。
ナビ自身は API を叩かない。

`layout.tsx` にクライアントを置いても、**配下のページはサーバーのまま**でいられる。
「'use client' は伝播するが、それは *子* に対してであって、
兄弟や親には及ばない」という点が実装して腑に落ちた。

### 実測⑨：Server Actions を選んだら LAN 公開が起動オプション1つで済む

（発表で触れるかは任意）

```
クライアントから直接 API を叩く設計だった場合:
  スマホのブラウザ → http://127.0.0.1:8000 を叩こうとして失敗
  → バックエンドを 0.0.0.0 で公開 + CORS 設定 + URL 書き換えが必要

Server Actions の場合:
  スマホのブラウザ → Next.js(3000) だけを叩く
  → Next.js が 127.0.0.1:8000 を呼ぶ（PC内部なのでそのまま動く）
  → バックエンドは無変更。CORS も不要
```

`npm run dev -- -H 0.0.0.0` だけで同じ Wi-Fi のスマホから開ける。

---

## S1 タイトル

**ラベル**：Phase 4 前半　｜　フロントエンド（Next.js・API接続）

**タイトル**：Next.js と API 接続
**サブ**：― 決めてから、作って確かめた

> 当初は「― 作る前に決めたこと」だったが、実装が動いたので変更した。
> 判断 → 実装 → 検証まで通した発表になったため。

2026-08-19 ｜ 30分

- **今日のトピック**： 境界 / 接続 / 画面 の3つの判断
- **ゴール**： 「境界・接続・画面」を自分のアプリでどうするか、自分で判断できるようになる

ブラウザ  →  Next.js  →  FastAPI  →  PostgreSQL
Phase 3 で右の3つを作った。今日はその左端を足す話

> **話す内容**
>
> 【1分30秒】導入
>
> ◆ 名乗り
> ・Phase 4 の前半、フロントエンドと API 接続を担当します
>
> ◆ 今日の立ち位置を図で示す
> ・下の帯を指す：ブラウザ → Next.js → FastAPI → PostgreSQL
> ・右の3つは前回まで（Phase 2 で DB、Phase 3 で API）
> ・今日はその左端、Next.js を足す話
>
> ◆ 今日話すこと・話さないこと
> ・話す：実装の前に何を決めたか、決めた通りに動いたか
> ・話さない：Next.js の全機能。1画面作るのに必要な判断だけ
> ・結果として3画面を作ったので、机上の話ではなく実際に測った数字を出します
>
> ◆ 一言添える
> ・先に言っておくと、途中で一度つまずいています。そこが今日の山場（S9）

---

## S2 学ぶこととゴール

**ラベル**：全体像 ｜ 今日の3テーマ
**サブ**：3つとも学ぶのは「判断」。答えはこの後で出す

| テーマ | 学ぶ判断 | こういう時に困る |
| :---- | :---- | :---- |
| 境界 | 処理を どこで動かすか | ページ全体が重くなり、表示前に空の画面が見える |
| 接続 | API を どう呼ぶか | 登録したのに、一覧が古いまま変わらない |
| 画面 | 機能を どこまで作るか | モックを7枚作ったが、半分は年に数回しか使わない |

**今日の立場**：今日は Next.js の全機能ではなく、画面を1つ作るときに必要な判断だけを扱う。

**ゴール**：「境界・接続・画面」を自分のアプリでどうするか、自分で判断できるようになる
そのために最後に、自分のアプリで決めることを3つ書き出す時間を取る

> **話す内容**
>
> 【1分30秒】全体像
>
> ◆ 3テーマを読み上げる
> ・境界＝処理をどこで動かすか
> ・接続＝API をどう呼ぶか
> ・画面＝機能をどこまで作るか
>
> ◆ 右の列（困る時）を強調する
> ・この3つは、どれも自分が実際に困った／困りかけたこと
> ・「ページ全体が重くなる」「登録したのに一覧が古いまま」
> ・「モックを7枚作ったが半分は年に数回しか使わない」
>
> ◆ 形式の予告
> ・各テーマ3枚ずつ。なぜ・困る時 → どう対応 → 実例
> ・前回（第五回）と同じ形式にしています
>
> ◆ ゴールの確認
> ・学ぶのは機能ではなく判断
> ・最後に、自分のアプリで決めることを3つ書き出す時間を取ります

---

## S3 Next.js はどこにいるか

**ラベル**：ARCHITECTURE ｜ 3分
**サブ**：Phase 3 で作った層の左に、もう1つ層が乗る

**層の図**

| 層 | 役割 |
| :---- | :---- |
| ブラウザ | 画面を見る・操作する |
| **Next.js** | 画面を組み立てる／HTTP の入口 |
| FastAPI | API（Phase 3 で作った） |
| PostgreSQL | 永続化（Phase 2 で作った） |

**要点**

- Next.js は「画面を作るツール」だけではなく、サーバーとしても動く。
- だから API を呼ぶ経路が2つできる。Next.js から呼ぶか、ブラウザから直接呼ぶか。
- どちらで呼ぶかで、書き方も見え方も変わる。これが今日の1つ目「境界」の話。
- 小さいアプリなら経路は1つでよい。分ける理由があるときだけ分ける。

> **話す内容**
>
> 【3分】Next.js はどこにいるか
>
> ◆ 4層の図を上から順に
> ・ブラウザ＝画面を見て操作する
> ・Next.js＝画面を組み立てる。HTTP の入口
> ・FastAPI＝Phase 3 で作った API
> ・PostgreSQL＝Phase 2 で作った DB
>
> ◆ ここが最初の発見
> ・Next.js を「フロントを作るツール」だと思っていた
> ・実際はサーバーとしても動く。Node のプロセスが立つ
> ・npm run dev すると localhost:3000 でサーバーが起動している
>
> ◆ だから経路が2つできる
> ・(1) ブラウザ → Next.js（サーバー）→ FastAPI
> ・(2) ブラウザ → FastAPI に直接
> ・どちらを選ぶかで、書き方も、ブラウザから見える情報も変わる
>
> ◆ 今日の1つ目のテーマにつなぐ
> ・この「どちらで動かすか」が、次の「境界」の話
>
> ◆ 補足（聞かれたら）
> ・小さいアプリなら経路は1つでいい。分ける理由があるときだけ分ける
> ・自分は(1)に統一した。理由は S8 で話します

---

## S4 境界：なぜ・困る時

**ラベル**：境界 ｜ サーバーとクライアント
**タイトル**：なぜ・困る時：ページ全体が重くなった

| | 意味 | ブラウザに JavaScript を送るか |
| :---- | :---- | :---- |
| サーバーコンポーネント | サーバーで HTML を組み立ててから送る | 送らない |
| クライアントコンポーネント | ブラウザ上で JavaScript が動いて描画する | 送る |

**なぜ必要か**：既定はサーバー。`'use client'` と書いた時だけクライアントになる。この1行で、送るものが変わる。

**こういう時に困る**：ページの先頭に `'use client'` を書いた

- 配下のコンポーネントが全部クライアントになる
- 支出42件の一覧も、ブラウザ側で組み立てることになる
- 表示までに4往復。一瞬「空っぽの画面」が見える

```
app/expenses/page.tsx

'use client'

export default function Page() {
  // ここから下は全部クライアント扱い
  return <ExpenseList items={...} />
}
```

> **話す内容**
>
> 【2分】境界：なぜ・困る時
>
> ◆ 表を読む前に一言
> ・「サーバーコンポーネント」は名前が紛らわしいと思っている
> ・サーバーでしか動かない、という意味ではない
> ・本質は右の列＝ブラウザに JavaScript を送るかどうか
>
> ◆ 表の右列を指す
> ・サーバー：送らない。HTML を組み立ててから渡す
> ・クライアント：送る。ブラウザ上で JS が動いて描画する
>
> ◆ 既定はサーバー
> ・何も書かなければサーバー
> ・'use client' と書いた時だけクライアントになる
> ・この1行で、ブラウザに送るものが変わる
>
> ◆ 困る例（右下のコード）
> ・ページの先頭に 'use client' を書いてしまった場合
> ・配下のコンポーネントが全部クライアント扱いになる
> ・支出42件の一覧も、ブラウザ側で組み立てることになる
> ・表示までに往復が増えて、一瞬「空っぽの画面」が見える
>
> ◆ つなぎ
> ・ではどこに書けばいいのか、が次の1枚

---

## S5 境界：どう対応するか

**ラベル**：境界 ｜ サーバーとクライアント
**タイトル**：どう対応するか：判断は1問だけ

### ブラウザでしか出来ないことを使うか？

**「ブラウザでしか出来ないこと」とは**

- `onClick` / `onChange` / `onSubmit`（操作への反応）
- `useState` / `useReducer`（状態を持つ）
- `useEffect`（副作用）
- `localStorage` / `window` / `document`

| 答え | どうする |
| :---- | :---- |
| Yes | クライアント（`'use client'` を書く） |
| No | サーバー（既定。何も書かない） |

**原則：`'use client'` は葉に付ける。根に付けない**
ページ（根）に `'use client'` を書くと配下が全部クライアントになる。
ただし layout の中にクライアント部品を「置く」のは別。その部品だけがクライアントで、children はサーバーのまま。

**今日の一手**：最初に作る画面で、クライアントが必要な部品を1つだけ決める

**結論**：既定はサーバー。操作がある部品だけをクライアントに切り出す。

> **話す内容**
>
> 【2分】境界：どう対応するか
>
> ◆ 判断は1問だけ、と言い切る
> ・「ブラウザでしか出来ないことを使うか？」
> ・Yes ならクライアント、No ならサーバー（＝何も書かない）
>
> ◆ 左のリストを読む
> ・onClick / onChange / onSubmit ＝ 操作への反応
> ・useState ＝ 状態を持つ
> ・useEffect ＝ 副作用
> ・localStorage / window / document
>
> ◆ 原則（右下のボックス）
> ・'use client' は葉に付ける。根に付けない
> ・根に付けると配下が全部クライアントになる（S4 の失敗パターン）
>
> ◆ ここは混同しやすいので丁寧に
> ・「layout にクライアント部品を置く」のは、根に 'use client' を書くのとは別
> ・自分のアプリのナビがまさにこれ
> ・ナビは現在地で色を変えるので usePathname が要る → クライアント
> ・でも layout に置いても、children の3画面はサーバーのまま
> ・つまり影響するのは「その部品自身」だけ
>
> ◆ 今日の一手
> ・最初に作る画面で、クライアントが必要な部品を1つだけ決める

---

## S6 境界：対応の実例

**ラベル**：境界 ｜ サーバーとクライアント
**タイトル**：対応の実例：作って測ってみた
**サブ**：3画面を実装した結果。クライアントにしたのは「操作がある部品」だけ

| 画面 | サーバー（表示だけ） | クライアント（操作がある） |
| :---- | :---- | :---- |
| ホーム | 集計・カテゴリ別グラフ・支出一覧・ページ送り | 入力フォーム・絞り込み・行内編集 |
| 月次清算 | 集計・送金額・内訳・月ナビ・過去履歴 | 「確認する」ボタン |
| PayPay 取り込み | 未判定の一覧・判定済み履歴 | CSV 選択・1行ごとの判定 |

### 3画面を作り終えた時点の内訳と、ホーム画面での計測

| 計測項目 | 結果 |
| :---- | :---- |
| ファイル数（サーバー / クライアント） | **17 / 8** |
| ブラウザから叩いた API の数 | **0 件** |
| サーバーが返す HTML に「¥ 16,640」 | **含まれる** |

**つまり**：ブラウザは API を1回も呼んでいないのに、データが表示されている。
クライアントにしたのは入力・絞り込み・ボタンなど「操作がある部品」8つだけ。

**結論**：画面ではなく部品で決める。表示はサーバー、操作はクライアント。

> **話す内容**
>
> 【2分30秒】境界：実例
>
> ◆ 上の表：3画面の振り分け
> ・左がサーバー、右がクライアント
> ・注目してほしいのは、行が「画面」なのに中身は「部品」で分かれていること
> ・画面単位ではなく部品単位で決めた
>
> ◆ 意外だった2つ（聞かれなくても話す）
> ・カテゴリ別グラフはサーバーのままにできた
>   → ライブラリを使わず CSS の width % で棒を描いたので JS が要らなかった
> ・絞り込みと月の切り替えもサーバーのまま
>   → useState ではなく URL に持たせた（/?category=1）ので <Link> の遷移で済む
>
> ◆ 下の実測表
> ・ファイル数は サーバー17 / クライアント8
> ・ホーム画面を開いてブラウザが叩いた API は 0 件
> ・サーバーが返す HTML に「¥16,640」がそのまま入っている
>
> 【実演】ここで curl の結果を見せる
> ・curl http://localhost:3000 | grep 16,640
> ・ブラウザの Network タブを開いても API 通信が1件も出ないことを見せてもいい
> ・これが S4 で言った「JS を送らない」の実物
>
> ◆ 結論
> ・画面ではなく部品で決める。表示はサーバー、操作はクライアント

---

## S7 接続：なぜ・困る時

**ラベル**：接続 ｜ データの取得と更新
**タイトル**：なぜ・困る時：登録したのに一覧が変わらない

**なぜ必要か**：取得と更新は別の問題として考える必要がある。同じ fetch でも、扱いが違う。

**こういう時に困る**：支出を登録した

- DB には入っている（psql で確認できる）
- API も 201 を返している
- でも画面の一覧は古いまま

```
よくある書き方

const res = await fetch(url)
const data = await res.json()

これだけでは、更新後に
再取得されるとは限らない
```

**この症状から切り分けるべきこと**

- キャッシュされていて古い結果が返っているのか
- 再取得の指示を出していないだけなのか
- そもそも取得と更新で別の経路を使うべきなのか
- （使っているバージョンで、既定の動きが変わっていないか）

> 最後の1点が今日の山場。ここで一度つまずいた。

> **話す内容**
>
> 【2分】接続：なぜ・困る時
>
> ◆ 症状から入る
> ・支出を登録した。DB には入っている。psql で確認できる
> ・API も 201 を返している
> ・なのに画面の一覧が古いまま
>
> ◆ 中央のコードを指す
> ・よくある書き方。fetch して json にする
> ・これだけでは、更新後に再取得されるとは限らない
>
> ◆ 切り分けるべき4点（右のリスト）
> ・キャッシュされて古い結果が返っているのか
> ・再取得の指示を出していないだけなのか
> ・そもそも取得と更新で別の経路を使うべきなのか
> ・使っているバージョンで既定の動きが変わっていないか
>
> ◆ 伏線を張る
> ・最後の1点が今日の山場。ここで自分は一度つまずいた
> ・その話は2枚あと（S9）でします
>
> ◆ このテーマの立場
> ・取得と更新は別の問題として考える必要がある
> ・同じ fetch でも扱いが違う、というのが次の枚

---

## S8 接続：どう対応するか

**ラベル**：接続 ｜ データの取得と更新
**タイトル**：どう対応するか：取得と更新で経路を分ける

| 操作 | 経路 | 書き方 |
| :---- | :---- | :---- |
| 表示（GET） | サーバーコンポーネント | `async function` にして直接 `await` する |
| 変更（POST / PATCH / DELETE） | Server Actions | `'use server'` を書き、最後に `refresh()` を呼ぶ |

```
表示：サーバーコンポーネント

export default async function Page() {
  const data = await apiGet('/api/expenses/')
  return <ExpenseList items={data.items} />
}
```

```
変更：Server Actions

'use server'
export async function createExpense(fd) {
  await fetch(API + '/api/expenses/', {...})
  refresh()
}
```

### 実際に支出を1件登録したときの変化（ページ遷移なし）

| | 合計 | 件数 | 外食カテゴリ | 送金額 | ブラウザのAPI呼び出し |
| :---- | :---- | :---- | :---- | :---- | :---- |
| 送信前 | ¥20,288 | 9 件 | ¥2,800（1件） | ¥1,896 | 0 |
| 送信後 | **¥22,868** | **10 件** | **¥5,380（2件）** | **¥3,186** | **0** |

集計・グラフ・一覧が同時に更新される。個別に取り直すコードは書いていない（`refresh()` 1行だけ）。

**結論**：表示はサーバーで `await`、変更は Server Actions。更新後は `refresh()`。

> **話す内容**
>
> 【3分】接続：どう対応するか
>
> ◆ 表：経路を分ける
> ・表示（GET）＝サーバーコンポーネント。async function にして直接 await
> ・変更（POST/PATCH/DELETE）＝Server Actions。'use server' を書いて、最後に refresh()
>
> ◆ 左右のコードを対比して見せる
> ・左：Page が async。await apiGet して、その結果を渡すだけ
> ・右：'use server' の関数。fetch した後 refresh() を呼ぶ
>
> ◆ Server Actions を選んだ理由（3つ。ここは自分の判断として話す）
> ・API の URL がブラウザに出ない。lib/api.ts に server-only を付けてある
> ・CORS の設定が要らない。ブラウザは Next.js しか叩かないから
> ・Phase 5 で JWT を HttpOnly Cookie に入れるとき、扱いがサーバー側で完結する
>
> ◆ 下の実測表を読む
> ・支出を1件登録した前後。ページ遷移はしていない
> ・合計 ¥20,288 → ¥22,868、件数 9 → 10
> ・外食カテゴリ ¥2,800（1件）→ ¥5,380（2件）
> ・送金額 ¥1,896 → ¥3,186
> ・ブラウザの API 呼び出しは前後とも 0
>
> ◆ ここが言いたいこと
> ・集計・グラフ・一覧・送金額が同時に更新されている
> ・個別に取り直すコードは1行も書いていない。refresh() 1行だけ
>
> 【実演】余裕があれば実際に登録して見せる
> ・表の数字は測定したときの記録なので、その場で実演すると今の値から動きます
> ・見せ方：合計と件数を読み上げてから登録 → 数字が一斉に変わるのを指す

---

## S9 接続：対応の実例 ★山場★

**ラベル**：接続 ｜ データの取得と更新
**タイトル**：対応の実例：調べたら記事と違っていた

**条件**：検索で出てきた記事を見ながら書こうとした。書き方が記事どうしで食い違っていた。

| 項目 | 記事に多い情報（13〜14） | 実際（16.3.1） |
| :---- | :---- | :---- |
| fetch のキャッシュ | 既定でキャッシュされる | **キャッシュされない** |
| キャッシュ回避 | `{ cache: 'no-store' }` を明示 | **不要（既定がその動き）** |
| 更新後の画面反映 | `router.refresh()` をクライアントで | **`refresh()` を Server Action 内で** |
| 変更処理 | クライアントから `fetch(POST)` | **Server Actions** |

### しかも、Next.js 自身が警告していた

― `npm run dev` が `AGENTS.md` を自動生成する

```
# This is NOT the Next.js you know

This version has breaking changes — APIs,
conventions, and file structure may all
differ from your training data. Read the
relevant guide in node_modules/next/dist/
docs/ before writing any code.
```

**読み取れること**

- フレームワーク自身がバージョン差の問題を認めている
- ドキュメントが `node_modules` に同梱されている
- 読んだ結果、動的ルートの `params` が Promise だと分かった

**結論**：バージョンを確認して、公式を一次情報にする。記事は補助に使う。

> **話す内容**
>
> 【2分30秒】接続：実例（今日一番伝えたい話）
>
> ◆ 何が起きたか
> ・検索で出てきた記事を見ながら書こうとした
> ・記事どうしで書き方が食い違っていた
> ・調べたら、バージョンで作法が変わっていた
>
> ◆ 表を読む（左が記事、右が実際）
> ・fetch のキャッシュ：既定でキャッシュされる → されない
> ・キャッシュ回避：no-store を明示 → 不要
> ・更新後の反映：router.refresh() をクライアントで → refresh() を Server Action 内で
> ・変更処理：クライアントから fetch(POST) → Server Actions
>
> ◆ 決め手になったもの（右のコード）
> ・npm run dev したら AGENTS.md というファイルが勝手に生成されていた
> ・中身を読む：「This is NOT the Next.js you know」
> ・「訓練データと違うかもしれない。node_modules の中のドキュメントを読め」
> ・フレームワーク自身がバージョン差の問題を認めている
>
> 【実演】実物を見せる
> ・cat frontend/AGENTS.md
>
> ◆ 読んだ結果わかったこと
> ・ドキュメントが node_modules に同梱されている
> ・動的ルートの params が Promise になっていた（await が要る）
> ・記事のまま書いていたら動かないコードになっていた
>
> ◆ 結論
> ・バージョンを確認して、公式を一次情報にする。記事は補助
> ・これは Next.js に限らず、他のライブラリでも同じだと思う

---

## S10 画面：なぜ・困る時

**ラベル**：画面 ｜ 作る範囲を決める
**タイトル**：なぜ・困る時：7画面のモックを作ったが多すぎた
**サブ**：「どのページを作るか」の話。コンポーネントの分け方（Feature-based など）は Phase 4 後半のテーマ

> ラベルを「どこまで作るか」から「作る範囲を決める」に変更した。
> 後半担当者の範囲（コンポーネント設計）と混同されないようにするため。

**なぜ必要か**：作る前に「作らないもの」を決めないと、使われない画面に時間を使う。

**こういう時に困る**：設計フェーズでモックを7枚作った。実装量を見積もったら 12〜16 時間だった。

| 当初の画面 | 使用頻度 | 実装の重さ |
| :---- | :---- | :---- |
| ダッシュボード | 毎日 | 重い |
| 支出一覧 | 週1〜月1 | 中 |
| 月次清算 | 月1 | 中 |
| PayPay 取り込み | 週1〜月1 | 重い |
| カテゴリ管理 | **年に数回** | 中 |
| ログイン | （認証は Phase 5） | 中 |
| アカウントメニュー | **ほぼ使わない** | 軽い |

> 使用頻度を書き出したら、半分は年に数回しか使わない画面だった。

> **話す内容**
>
> 【2分】画面：なぜ・困る時
>
> ◆ 最初に範囲を断る
> ・ここは「どのページを作るか」の話
> ・コンポーネントをどう分けるか（Feature-based など）は Phase 4 後半のテーマ
>
> ◆ 経緯
> ・設計フェーズでモックを7枚作った
> ・実装量を見積もったら 12〜16 時間だった
> ・多いと思って、使用頻度を書き出してみた
>
> ◆ 表の真ん中の列を指す
> ・ダッシュボード：毎日
> ・支出一覧：週1〜月1
> ・月次清算：月1
> ・PayPay 取り込み：週1〜月1
> ・カテゴリ管理：年に数回
> ・アカウントメニュー：ほぼ使わない
>
> ◆ 気づき
> ・半分は年に数回しか使わない画面だった
> ・設計しているときは全部必要に見えていた
> ・使用頻度を書き出すまで気づかなかった
>
> ◆ 問題提起
> ・作る前に「作らないもの」を決めないと、使われない画面に時間を使う

---

## S11 画面：どう対応するか

**ラベル**：画面 ｜ 作る範囲を決める
**タイトル**：どう対応するか：使用頻度で切る

画面を1つずつ、この3問にかける。No が多いほど独立させる必要がない。

1. その画面を週1回以上使うか
2. 他の画面に入れられないか
3. その画面が無いと運用が止まるか

| No の数 | やること |
| :---- | :---- |
| 0 | そのまま作る |
| 1〜2 | 他の画面に統合する |
| 3 | 作らない（API は残す） |

**大事な点：機能を消すのではなく、画面を作らない**
API とカラムは残したまま、UI だけ作らない。必要になったら画面を足すだけで済む。

**今日の一手**：自分のモックで、使用頻度が一番低い画面を1つ挙げる

**結論**：使用頻度が低い画面は独立させない。API は残して UI だけ作らない。

> **話す内容**
>
> 【1分30秒】画面：どう対応するか
>
> ◆ 3問を読み上げる
> ・その画面を週1回以上使うか
> ・他の画面に入れられないか
> ・その画面が無いと運用が止まるか
>
> ◆ 表の使い方
> ・No が多いほど、独立した画面にする必要がない
> ・No が3つなら作らない
>
> ◆ ここが一番大事（中央のボックス）
> ・機能を消すのではなく、画面を作らない
> ・API とカラムは残したまま、UI だけ作らない
> ・必要になったら画面を足すだけで済む
>
> ◆ 自分の例で補足
> ・カテゴリの API は Phase 3 で4本作ってある
> ・is_archived カラムもある
> ・でも管理画面は作らなかった。追加は入力欄の「+ 新規」で足りる
>
> ◆ 今日の一手
> ・自分のモックで、使用頻度が一番低い画面を1つ挙げる

---

## S12 画面：対応の実例

**ラベル**：画面 ｜ 作る範囲を決める
**タイトル**：対応の実例：7画面 → 3画面

| 当初の画面 | 判断 |
| :---- | :---- |
| ダッシュボード | 残す（ホームに改称） |
| 支出一覧 | **ホームに統合** |
| 月次清算 | 残す |
| PayPay 取り込み | 残す |
| カテゴリ管理 | **廃止** |
| ログイン | Phase 5 に送る |
| アカウントメニュー | **廃止** |

**廃止した機能の行き先**

- 支出一覧 → ホーム下部に統合。編集は行内で
- カテゴリ追加 → 入力欄の「+ 新規」
- 並び替え・アーカイブ → 作らない
- 表示名・パスワード変更 → Phase 5

**得たもの**：実装が概ね半分になり、Phase 5 に早く進める。

**代償**

- カテゴリの並び替えが UI からできない（API は残してある）
- モックを作り直す手間が発生した（PC 版とスマホ版で6枚）

**結論**：使用頻度で切る。API を残しておけば、後から画面を足せる。

> **話す内容**
>
> 【2分】画面：実例
>
> ◆ 表を上から
> ・ダッシュボード → 残す（ホームに改称）
> ・支出一覧 → ホームに統合
> ・月次清算、PayPay 取り込み → 残す（核心機能）
> ・カテゴリ管理 → 廃止
> ・ログイン → Phase 5 に送る（認証が前提なので）
> ・アカウントメニュー → 廃止
>
> ◆ 廃止した機能の行き先（左下）
> ・消したのではなく、置き場所を変えただけだと伝える
> ・支出一覧 → ホーム下部。編集は行内で直接
> ・カテゴリ追加 → 入力欄の「+ 新規」
> ・並び替え・アーカイブ → 作らない（API は残っている）
>
> ◆ 得たもの
> ・実装が概ね半分になった
> ・Phase 5（認証）に早く進める
>
> ◆ 代償も正直に話す
> ・カテゴリの並び替えが UI からできない
> ・モックを作り直す手間が発生した。PC 版とスマホ版で6枚
> ・ただ、実装前に気づけたので手戻りは小さかった
>
> ◆ 結論
> ・使用頻度で切る。API を残しておけば後から画面を足せる

---

## S13 自分のアプリで決めること

**ラベル**：自分のアプリへ ｜ 3つを決める
**サブ**：手元のモックかコードを開いて、3つを1行ずつ書く

1. **境界**　最初に作る画面で、`'use client'` が必要な部品を1つ挙げる
2. **接続**　更新後に画面を更新する手段を決めたか（使う関数名を1つ書く）
3. **画面**　作らないと決めた画面はあるか（1つ挙げる。無ければ「なし」）

**まだコードがない人**：作る予定の画面で「どこをクライアントにするか・どう更新するか・作らない画面はどれか」を紙に書く。

書けたら、1つだけ共有してください（パス可）

> **話す内容**
>
> 【3分】ワーク
>
> ◆ 進め方を先に言う
> ・3分取ります。手元のモックかコードを開いてください
> ・3つを1行ずつ書く。書けたら1つだけ共有してもらいます（パスも可）
>
> ◆ 3問を読み上げる
> ・境界：最初に作る画面で 'use client' が必要な部品を1つ挙げる
> ・接続：更新後に画面を更新する手段を決めたか。使う関数名を1つ書く
> ・画面：作らないと決めた画面はあるか。1つ挙げる。無ければ「なし」
>
> ◆ まだコードがない人へ（右下）
> ・紙に書くだけでいい
> ・作る予定の画面で、どこをクライアントにするか／どう更新するか／作らない画面はどれか
>
> ◆ 時間を計る
> ・3分経ったら声をかける
> ・巡回して詰まっている人がいたら、自分の例を出して助ける
>   例：自分は「入力フォーム」「refresh()」「カテゴリ管理画面」と書いた
>
> ◆ 共有のとき
> ・1人1つでいい。全員に当てない

---

## S14 まとめ

**ラベル**：まとめ ｜ 明日やる1つ
**タイトル**：明日やる1つを決める

**今日のキーワード**：境界 / 接続 / 画面

- **境界**　既定はサーバー。操作がある部品だけ `'use client'` を付ける
- **接続**　表示は `await`、変更は Server Actions。更新後に `refresh()`
- **画面**　使用頻度が低い画面は作らない。API は残しておく

### 3つ全部ではなく、決めた1つだけ。

**今日の副産物**：バージョンを確認して公式を読む。記事は補助に使う。

次回：Phase 4 後半（コンポーネント設計・UI ライブラリ）

> **話す内容**
>
> 【1分30秒】まとめ
>
> ◆ 3つを振り返る
> ・境界：既定はサーバー。操作がある部品だけ 'use client'
> ・接続：表示は await、変更は Server Actions。更新後に refresh()
> ・画面：使用頻度が低い画面は作らない。API は残しておく
>
> ◆ ここを強調する
> ・3つ全部やろうとしないでほしい
> ・決めた1つだけ、明日やる
>
> ◆ 副産物
> ・バージョンを確認して公式を読む。記事は補助に使う
> ・Next.js 以外でも効く話だと思う
>
> ◆ 締め
> ・次回は Phase 4 後半、コンポーネント設計と UI ライブラリ
> ・質問があれば
>
> ◆ 想定質問への備え
> ・「shadcn/ui は使わないのか」
>   → 今回は入れなかった。3画面で汎用部品が出てこなかったから。後半のテーマ
> ・「型は生成しないのか」
>   → OpenAPI からの生成も検討したが、型が10個程度だったので手書きにした
> ・「なぜ Server Actions？普通に fetch ではだめ？」
>   → S8 の3つの理由。特に Phase 5 の Cookie 認証を見据えて
> ・「エラーはどう出している？」
>   → 422 は detail[].loc[1] を見て該当フィールドの下に日本語で出している
