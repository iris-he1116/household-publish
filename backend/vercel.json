{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["hnd1"],
  "functions": {
    "index.py": {
      "maxDuration": 60,
      "excludeFiles": "{tests/**,.venv/**,.pytest_cache/**,.mypy_cache/**,.ruff_cache/**,scripts/**}"
    }
  }
}
